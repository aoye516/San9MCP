# 参与开发

## 这个项目的规矩不多，但每条都是血泪换来的

### 0. ⛔⛔ 禁止盲点：不知道点哪里就先定位；定位不到就不动手（最高优先级）

**这条是本项目唯一一条"违反即返工"的硬禁令，2026-09-19 由维护者当面确立。**

**禁止**任何形式的"猜一个坐标点一下看看"：

- ❌ `CLOSE_POINTS = [(350,250), (250,290), ...]` —— 关不掉就试下一个落点
- ❌ `CITY_CLICK_ALT = [(640,480), (632,486), ...]` —— 同一点不灵就依次试 5 个
- ❌ `ITEM_FALLBACK = (1117, 334)` —— OCR 读不到就拿硬编码坐标点
- ❌ `for band in (("red",...), ("red",...), ("brown",...))` —— 换条带继续找按钮
- ❌ 任何 `for pt in 候选列表:` 形式的点击循环

**必须**：每一次点击之前先回答「**我凭什么知道要点这里？**」—— 答案只能是下面三种之一：

| 定位方式 | 例子 |
|---|---|
| **程序化检测** | `find_button`（连通块 + 宽高比/面积/填充率判据）、OCR 文字框、模板匹配 |
| **游戏语义推导** | 选中列表某行后地图会滚动让该设施**落在屏幕正中** → 所以点 `CITY_CENTER` 是对的 |
| **实测记录 + 运行时复核** | 点完立刻读一个**能分辨**的判据（`commanded` 标题 / `go` 锚点 / 底栏提示） |

**定位不到 → 什么都不点**，返回 `ok:false` + 截图 + 「我看到了什么」，交给调用方或人来决定。
**试一次是动作，试两次是确认，试到第三次就是把鼠标交给运气。**

> 为什么定这么死：旧版 `atoms.recover()` 一次调用最坏会**盲点约 45 次鼠标**
> （7 个落点 × 2 轮 + 4 条带 × 3 轮），`open_command_menu` 最坏约 **75 次**。
> 屏幕上的表现就是"打开一个没用的弹窗、乱点半天、关了又开"。

### 1. 先读说明书，不许靠截图猜机制

界面语义 / 命令条件 / 事件含义，一律先查机制笔记或游戏说明书本身（作者的机制笔记 `docs/04` 含维护过程记录，**不随本发布包分发**）
（每一条都带说明书页码出处）。查不到再实测，**实测出来的结论必须补回该文件**。

报告和注释里要分清三种东西：**说明书写的** / **我实测的** / **我推断的**。

### 2. 不许从截图上目测坐标

显示端会把图缩放到 ~0.84，目测出来的坐标在 (1200,860) 附近误差 > 200px。

- 要目测 → 用 `annotate.shot(tag)`（原分辨率 + 100px 网格 + 真实坐标标签）
- **首选程序化检测**：OCR 文字框 / 色块连通块 / 模板匹配

### 3. 一步一验，禁止长脚本

**一次工具调用 = 一个动作。** 做完立刻看截图确认，再走下一步。

**三条补充（都是踩过才写的）：**

1. **"只读侦察"不是长脚本的豁免。** 只要会开界面 / 弹窗 / 点按钮，就是"动手"，
   就要一步一验。曾经写过 `probe_steps.py` / `probe_picker.py` 这类脚本，
   一次运行里串起"开菜单 → 点选将标签 → 点中止 → 点地图空白"，中间不停下来看图 ——
   屏幕上的表现就是乱点，最后把游戏卡在命令界面里。
2. **"计划被批准" ≠ "可以串成一个脚本跑"。** 计划里**每一条编号的步骤 = 一次独立的工具调用**。
   曾经拿着批准过的 3 步计划，直接写成一个 heredoc 一口气跑完。
3. **收尾必须是一个能验证的动作**，不许写"关闭/清理的兜底重试点"当收尾。
   **把界面留脏 = 任务失败**，哪怕数据拿到了 —— 收工的 `go` 锚点必须跟动手前一致。

长程循环（`cli.play --turns N`）只能作为**最终的能力验证**存在，
**不能拿来当探索手段** —— 中间一步没生效，后面全是连锁乱点，而脚本自己看不见。

### 4. 判据必须是内容，不是面积

- 判"菜单关掉了没有"必须跟**干净基线**比，不能跟"菜单开着那帧"比
- 判"菜单开没开"要 **OCR 读到菜单里的字**，不能靠"这块区域变化了多少像素"
  （右侧面板刷新也会让那片区域变化 >8000 像素 → 假阳性）

### 5. 失败必须留证 + 必须清理

任何工具失败时返回 `ok:false` + `failed_at` + `reason` + 截图路径。
**失败后界面会留在脏状态**（例如命令界面没关），下一个动作会因此崩 ——
所以失败路径里要调 `atoms.recover()`。

⚠️ **但 `recover()` 不是"万能清理"，它自己受第 0 条约束**：
- 它**只做"已定位的动作"**：先 `find_button` 找红色「中止」，找不到就
  `locate_blank_map_point` 定位一个平坦地图点，**两个都定位不到就什么都不做**
- 上限 **2 次**（实测退出要点两次「中止」：弹窗 → 命令界面 → 战略面），
  到顶**返回失败 + 留图**，**绝不换坐标继续试**
- 调用方拿到 `recovered:false` 时**必须停手**，把图给人看，不要再自己想办法

**顺序也重要：先留证，再清理。** 曾经把截图拍在 `recover` **之后** ——
拍的是恢复后的画面，等于没留证。

### 6. 绝不半执行

参数不够时就**一无所动**（连面板都不开），只返回缺什么 + 可选项。
留下"面板开着、参数填了一半"的脏状态，比直接失败更危险。

---

## 加一个新工具的标准流程

1. 在 `san9mcp/tools/` 对应分组里加 `@registry.tool(...)`，**schema 写全**（agent 只看得到 schema）
2. 用 `python scripts/mcp_probe.py call <工具名>` 实测
3. **四步验收判据**（缺一不可）：
   - ① 真实调用一次，返回 `ok:true`
   - ② **另一条独立证据**印证（`san9_journal` 有记录 / 面板数字真的变了）
   - ③ **换一个对象再跑一次**（换城 / 换命令 / 换武将），仍然 `ok:true`
   - ④ 失败时返回明确原因，不是静默或抛异常
4. 更新 `CHANGELOG.md`（发布包里的 `README.md` 是给使用者看的）

> **只跑通一次 = 跑通过，不等于能用。** 第 ③ 条是稳定性的唯一来源。

## 加能力加在哪

| 加什么 | 放哪 |
|---|---|
| 新的**读屏 / 点击 / OCR 能力** | `san9/` —— 这里是**唯一**实现处 |
| 新的**工具入口**（把已有能力包成 agent 可调） | `san9mcp/tools/` |
| 协议层改动 | `san9mcp/server.py` |

⛔ **不许在 `san9mcp/` 里写截图 / OCR / 点击逻辑** ——
否则会变成两份实现，早晚不一致。`san9mcp/` 只做翻译与校验。

## 提交

- 一个提交做一件事，message 说清**为什么**（"修 month 读成 null：OCR 把下旬读成下句"）
  比说清"改了什么"更有用
- 如果这次改动推翻了以前的结论，**在 commit message 里写明推翻了什么**

### ⛔ `git rm -f` 会连带删掉同目录的未跟踪文件（2026-09-19 实测踩到，丢了 7 个脚本）

删 `scripts/read_inren.py` 时用了 `git rm -f`。它删掉该目录最后一个**已跟踪**文件后，
git 顺手清掉"空"目录 —— 但目录里还有 7 个**未跟踪**的新脚本，
在 git 眼里不存在，被**一并物理删除，且不进回收站**。

已跟踪的能 `git checkout HEAD -- <dir>` 救回来；未跟踪的**没救**。

**规矩**：
1. **删单个文件不要用 `git rm`** —— 用 `git rm --cached <file>` + 手动删那一个文件，
   或者直接删文件再 `git add -A`。
2. **动 `git rm` / `git clean` / `git checkout --` 之前先看 `git status --porcelain`**：
   同目录有 `??` 就先提交或 stash 它们。
3. **收工前 `git status --porcelain` 应该是 0 行** —— 长期挂着一堆未跟踪的新文件，
   等于把它们放在任何 git 破坏性操作的射程里。
