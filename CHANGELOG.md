# Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.2.1] — 2026-09-20

**修一个会让长跑卡死的弹窗漏判。**

### 修复

- **`dialog.blocking()` 在弹窗挡住时会漏判。**
  旧写法 `s is not None and s < 0.8`，其中 `s` 只在锚点 `found=True` 时才取得到值 ⇒
  **`go` 锚点完全找不到时直接返回 `False`（= "没被挡"）**。
  实测「仕官待遇」弹窗正是这种（`go` 锚点 `found=False`、底栏被 `決定是否接受仕官` 占着）。

  后果：`san9_end_turn` 走不到 `stage="need_dialog"` 分支，把"被弹窗挡住"报成
  `第 1 旬没推进：演出画面持续超过 150 秒还没结束` —— **旬其实推进了**，
  只是弹窗挡着顶栏读不到。agent 会据此以为过旬失败，长跑直接卡死。

  现在**三段任一成立就算被挡**：

  | 段 | 条件 |
  |---|---|
  | ① | `go` 锚点找得到、分 < 0.8 |
  | ② | `go` 锚点分高，**但中央盖着一块亮底对话框**（确认框盖不住右下角的坑）|
  | ③ | **`go` 锚点找不到** ⇒ 看底栏是不是 `請選擇命令起點` |

  - ②复用 `san9/session.py` 的**同一套常量**（`DIALOG_WATCH` / `DIALOG_BRIGHT` /
    `DIALOG_MIN_BLOB` / `DIALOG_*_RANGE`），不另写一份。
  - 底栏 OCR 会把「請」读成「遥」⇒ 只匹配尾巴「命令起點」。
  - 新增 `dialog.blocking_detail()` 返回三段证据；`san9_end_turn` 的返回里带上 `how_judged`。

- 常规教训写进文档：**判据"取不到值" ≠ 判据说"没发生"** ——
  三态（真 / 假 / 读不到）必须分开，读不到时要有兜底判据，不能静默当成"假"。

### ⛔ 使用禁区

`blocking()` **不要在"过旬演出进行中"调用** —— 演出期间 `go` 锚点本来就找不到、
底栏也不是 `請選擇命令起點` ⇒ 会返回 `True`（假阳性）。
只在"演出已超时/结束后，判断是被弹窗挡住还是真没推进"时用。

### 验证

- 离线（把 `winio.grab` / `cmdscreen.read_hint` 换成存帧）：
  仕官弹窗 ×2 → `True`（旧版 `False`）· 干净战略面 / 发兵后战略面 → `False`（不误报）
  · 出征屏 / 目标表 → `True`（合理）。
- 真机：干净战略面 `blocking=False`、`go_score=0.9103 found=True`。

---

## [0.2.0] — 2026-09-20

**战略面基本玩法补齐**：v0.1 只有内政 + 輸送 + 观测；本版把人材与軍事两条命令线做完，
至此"选城 → 下命令 → 过旬 → 读结果"这套循环已经**全部可以交给 AI 跑**。
默认暴露的工具 **20 → 24**。

### 新增工具

| 工具 | 命令 | 说明 |
|---|---|---|
| `san9_move` | 人材 → 移動 | 把武将移动到自势力的另一座设施 |
| `san9_search` | 人材 → 探索 | 派人探索地域，找在野人才 / 宝物 |
| `san9_dengyong` | 人材 → 登庸 | 招募在野 / 他势力武将（两步：只读名单 → 选人提交）|
| `san9_deploy` | 軍事 → 出征 | **完整链路**：执行武将 →（可选）士兵数量 → 目标 → 方针 → 出兵 |

### 出征（`san9_deploy`）—— 本版最重的一条

全链路真机验收通过：

```
点城 → 軍事 → 出征 → 点「執行武將」→ 勾选 → 「決定」
  →（游戏自动填好 大將/陣形/船/士兵/派生数值）
  →（可选）点「士兵」→「輸入數字」对话框 → 清去 + 逐位点数字键 → 指定人数（如 8000）
  → 点出征屏「執行」→ 自动弹出「選擇對象」目标表（15 行）
  → 点某一行   → 直接弹出「決定方針」窗（不算先走地图）
  → 点方針窗「執行」= 真正出兵（不可逆）
  → 再到战略面点一次「進行」，部队开拔
```

- **默认 `dry_run=true`** ⇒ 只配好部队、把目标表与方针窗读给你看，**不发兵**。
- `dry_run=false` **必须同时给 `target_row`**，否则直接拒绝（不允许"点开目标表却不知道打谁"）。
- `expect_target` 用来核对目标城名：点进方针窗后用**大字号**回读「目標」比对，**对不上就停手**。
- 支持 `houshin={"追擊": "不許可"}` 改方针四组开关（敵接近時 / 自主撤退 / 追擊 / 事後命令）。

### 修复与加固

- **`atoms.recover` 退不出自绘窗** ⇒ 新增**分级退屏**（`deploy.step_back_out`）：
  按层选用该层已验证的那一个动作（方针窗→点「中止」/ 目标表→Esc / 出征屏→Esc …），
  每动一下重判层，屏幕没变就停手。失败清理不再把游戏停在半路。
- **"读不出 ≠ 不能操作"**（维护者指正）：`open_command_menu` 原来在左上标题读不出时直接停手，
  导致右侧列表里名字读不出的城整个不能用。改为**两级核实** ——
  标题读不出时改用"命令菜单真的开了没"确认点击生效（点空地图是开不出菜单的），
  设施身份按**右侧列表行号**认，并把 `title_unread` 明确标出来。
- 多处判据回归修复，统一到两条通则：
  **① 判据要用不随状态变的东西**（别用"数值区"、别用"当前高亮的那个字段"）；
  **② 一个弱特征不够**，必须配一条互斥的文案（底栏提示）当第二条件。

### 变更

- 版本串统一为 `0.2.0`（此前 `san9mcp/__init__.py` 与两个 server 不一致）。
- 文档里的本机绝对路径改为通用占位（`cwd: C:/path/to/san9mcp`）。
- 新增 `CHANGELOG.md` 与 `CONTRIBUTING.md`。
- `.gitignore` 补充运行产出类型。

---

## [0.1.0] — 2026-09-18

首个发布包。让外部 agent 能接入并看到一组已存在、可调用的 MCP 工具；
未完成的离线规划器不出现在默认 `tools/list` 中。

- 默认注册 28 项，暴露 20 项。
- 环境与启动：`san9_status` `san9_launch` `san9_focus`
- 观测：`san9_look` `san9_topbar` `san9_menu` `san9_panel` `san9_officers` `san9_shot`
- 情报：`san9_intel_officer` `san9_city_officers`
- 命令与时间：`san9_facility_command` `san9_transport` `san9_target_own_city`
  `san9_end_turn` `san9_dialog` `san9_journal` `san9_recover`
- 知识：`san9_docs`
- 通用只读 UI：`san9_ui_read`（仅 `command_menu` / `officer_picker`）
